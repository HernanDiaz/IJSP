#!/usr/bin/env python3
"""I-022: a record attempt with the configuration of two acceptances.

Usage: make_jobs.py attempt

A record is proved by its schedule, not by a p-value: no filter, no waves,
nothing accepted or rejected.

Two acceptances now stack: I-018 sends the second tabu call of each pair of
children to the better child, and I-021 keeps searching that child again for
as long as each call improves it. I-022 showed that I-018's gain reaches the
lower tail; this attempt does the same for I-021 while spending its runs where
our best is closest to the best known.

Two cells. `previous` is setup/ref_I-018.txt, the configuration I-021 was
measured against. `current` is setup/ref_I-021.txt. I-006's short list and
budget: ta29, ta30, ta23, ta22, 75 runs of 40 s per cell and instance, on fresh
seeds 3001 to 3075.
"""
import os
import sys

HERE = os.path.dirname(os.path.abspath(__file__))
EXPERIMENT = os.path.abspath(os.path.join(HERE, "..", ".."))
BASE = os.path.join(EXPERIMENT, "setup", "ref_I-018.txt")
REFERENCE = os.path.join(EXPERIMENT, "setup", "ref_I-021.txt")
FIRST_SEED = 3001          # fresh: no earlier batch has used these

RUNS_TOTAL = 75            # per instance and cell
RUNS_PER_JOB = 5           # so no job runs longer than about 13 minutes
BUDGET = {"20x20": 40, "30x15": 100, "30x20": 150}

SIZE = {}
BKS = {}
for row in open(os.path.join(EXPERIMENT, "taillard_bounds.csv")):
    f = row.strip().split(",")
    if f[0] != "instance":
        SIZE[f[0]] = f[1] + "x" + f[2]

# The shortlist: the instances where our best verified makespan is closest to
# the BKS (JOURNAL 2026-09-21). ta29 was matched once at 1625 in the long
# regime; ta30 is 11 away in this one.
SHORTLIST = ["ta29", "ta30", "ta23", "ta22"]
CELLS = ["previous", "current"]


def setup_text(instance, cell, runs, seed, tag):
    out = []
    for line in open(REFERENCE if cell == "current" else BASE).read().splitlines():
        if line.startswith("timelimit = "):
            line = "timelimit = %d" % BUDGET[SIZE[instance]]
        elif line.startswith("runs = "):
            line = "runs = %d" % runs
        elif line.startswith("seed = "):
            line = "seed = %d" % seed
        out.append(line)
    header = ("# GENERATED by iter/I-022/make_jobs.py, cell %s, %s, %s\n"
              "# (%s, %d s per run, %d runs, seed %d). Do not edit.\n"
              % (cell, instance, tag, SIZE[instance],
                 BUDGET[SIZE[instance]], runs, seed))
    return header + "\n".join(out) + "\n"


def main():
    if (sys.argv[1:2] or [""])[0] != "attempt":
        raise SystemExit(__doc__)
    setup_dir = os.path.join(HERE, "setups")
    os.makedirs(setup_dir, exist_ok=True)
    chunks = RUNS_TOTAL // RUNS_PER_JOB
    jobs = []
    for chunk in range(1, chunks + 1):
        seed = FIRST_SEED + RUNS_PER_JOB * (chunk - 1)
        for instance in SHORTLIST:
            for cell in CELLS:
                tag = "c%02d" % chunk
                path = os.path.join(setup_dir, "%s_%s_%s.txt" % (instance, cell, tag))
                with open(path, "w", newline="\n") as handle:
                    handle.write(setup_text(instance, cell, RUNS_PER_JOB, seed, tag))
                jobs.append("%s\tI-022_%s_%s\t%s" % (path, tag, cell, instance))
    jobs_path = os.path.join(HERE, "jobs_attempt.tsv")
    with open(jobs_path, "w", newline="\n") as handle:
        handle.write("# I-022 record attempt: %d instances x %d cells x %d runs, "
                     "in %d chunks of %d, seeds 1..%d\n"
                     % (len(SHORTLIST), len(CELLS), RUNS_TOTAL,
                        chunks, RUNS_PER_JOB, RUNS_TOTAL))
        handle.write("\n".join(jobs) + "\n")
    cpu = sum(BUDGET[SIZE[i]] * RUNS_TOTAL * len(CELLS) for i in SHORTLIST)
    print("attempt: %d jobs -> %s  (%.1f CPU-hours, %d runs in total)"
          % (len(jobs), os.path.relpath(jobs_path), cpu / 3600.0,
             RUNS_TOTAL * len(CELLS) * len(SHORTLIST)))


if __name__ == "__main__":
    main()
