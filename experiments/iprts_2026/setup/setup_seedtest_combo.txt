# SEEDING TEST (B) — combined warm bank: N2+N8 + OR-Tools seeds, max-min.
# A/B partner of setup_seedtest_n2n8.txt. Only creation.solutions-dir differs,
# so any delta is attributable to the added OR-Tools (CP-SAT) seeds.
# Short budget: 3 runs x 180 s (warm search plateau-stops well before that).

# GENERIC
algorithm = IPRTS
seed = 1
runs = 3

# PROBLEM
problem.hasbounds = no

# OBJECTIVE
objective = ijsp.makespan
evaluation.interval.comparison = LEX2
evaluation.interval.maximum = Component
evaluation.lamarckism = yes

# CODIFICATION
codification = ijsp.job-order
decodification = ijsp.job-order
decode.sgs = insertion
sgs.interval.comparison = LEX2

# CREATION: warm start from the COMBINED N2+N8+OR-Tools bank, max-min selection
creation = ijsp.solutions-file
creation.solutions-dir = experiments/iprts_2026/seeds/N2N8_ortools_x20
creation.seed-selection = maxmin
creation.randomratio = 0.1
creation.sgs = insertion

# ELITE POOL
pool.size = 16
pool.min-distance = 0.0325
pool.restart-patience = 26
pool.restart-perturbation = yes
pool.perturbation-strength = 0.0993
pool.seed-retries = 5

# PATH RELINKING
pr.neighbourhood = ijsp.makespan.n1
pr.max-steps = -1

# STOPPING CRITERION
noimprovement = 5000
timelimit = 180

# EVOLUTION REPORTING
evolution.unit = time
evolution.span = 1

# STATISTICS
statistics.1.metric = best
statistics.1.value = ijsp.makespan
statistics.2.metric = average
statistics.2.value = ijsp.makespan

# LOCAL SEARCH (irace-tuned TS-N2 kernel)
localsearch = tabu-search
localsearch.estimation-guide = no
localsearch.filter = no
localsearch.max-time = 2
localsearch.neighbourhood = ijsp.makespan.n2
neighbourhood.estimator = heads&tails
localsearch.lamarckism = yes
localsearch.bad-iterations = 255
localsearch.tabu-size.min = 3
localsearch.tabu-size.max = 10
localsearch.max-rounds = 24

# skip robustness analysis
postexecution = no
